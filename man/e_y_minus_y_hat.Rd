% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e.R
\name{e_y_minus_y_hat}
\alias{e_y_minus_y_hat}
\title{Residuals (\eqn{\mathbf{e} = \mathbf{y} - \mathbf{\hat{y}}})}
\usage{
e_y_minus_y_hat(y, y_hat = NULL, X = NULL, beta_hat = NULL)
}
\arguments{
\item{y}{Vector or \code{n} by \code{1} matrix.
The vector
\eqn{\mathbf{y}}
is an \eqn{n \times 1} vector
of observations on the regressand variable.}

\item{y_hat}{Vector or \code{n} by \code{1} matrix.
\eqn{n \times 1} vector
of predicted values of \eqn{\mathbf{y}}
(\eqn{\mathbf{\hat{y}}}).}

\item{X}{Matrix.
The data matrix
\eqn{\mathbf{X}}
is an \eqn{n \times k} matrix
of \eqn{n} observations
of \eqn{k} regressors,
which includes a regressor
whose value is 1 for each observation.}

\item{beta_hat}{Vector or \code{k} by \code{1} matrix.
The vector
\eqn{\boldsymbol{\hat{\beta}}}
is a \eqn{k \times 1} vector of estimates
of \eqn{k} unknown regression coefficients.}
}
\value{
Returns residuals (\eqn{\mathbf{e}}),
that is,
the difference between
the observed and predicted value of \eqn{\mathbf{y}}
(\eqn{\mathbf{e} = \mathbf{y}} - \eqn{\mathbf{\hat{y}}}).
}
\description{
Calculates residuals using
\deqn{
    \mathbf{e}
    =
    \mathbf{y}
    -
    \mathbf{\hat{y}}
    =
    \mathbf{y}
    -
    \mathbf{X}
    \boldsymbol{\hat{\beta}}.
  }
}
\details{
If \code{y_hat = NULL},
the \code{y_hat} vector is computed
using \code{\link[=y_hat_Xbeta_hat]{y_hat_Xbeta_hat()}}
with \code{X} as a required argument
and \code{beta_hat} as an optional argument.
If \code{y_hat} is provided,
\code{X} and \code{beta_hat} are not needed.
}
\references{
\href{https://en.wikipedia.org/wiki/Errors_and_residuals}{Wikipedia: Errors and Residuals}
}
\seealso{
Other residuals functions: 
\code{\link{e_My}()},
\code{\link{e}()}
}
\author{
Ivan Jacob Agaloos Pesigan
}
\concept{residuals functions}
